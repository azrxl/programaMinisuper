#pragma once
#include <fstream>
#include "Lista.h"

/*
class Archivo
{
public:
	Archivo();
	virtual ~Archivo();
	void guardarLista(Lista*);
	void cargarLista(std::string nombre);

private:
	std::string nombreArchivo;
};

*/